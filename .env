# Mini Praisells Environment Configuration
# Copy this file to .env and update the values

# MongoDB Connection (replace with your MongoDB URI)
MONGO_URI=mongodb://localhost:27017

# Server Configuration
PORT=3000

# App Configuration
NODE_ENV=development

# Note: No Pi API key needed since we're using virtual currency (appraiCENTS)
# This removes the need for real payment processing
